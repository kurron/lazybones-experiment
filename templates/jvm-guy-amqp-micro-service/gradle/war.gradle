apply plugin: 'war'

war.manifest.attributes( "Implementation-Title": implementationName, "Implementation-Version": project.version )

dependencies {
// not currently needed
//    providedRuntime "javax.servlet:servlet-api:${servletApiVersion}"
}
